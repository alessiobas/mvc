{% extends 'base.html.twig' %}

{% block title %}Library{% endblock %}

{% block content %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 33%; font: 18px/1.5 sans-serif; float: left; text-align: center; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

{% for book in books %}

<div class="example-wrapper">
    <a href="{{ path('book_id', { 'id': book.getId }) }}">
        <h4> Title: {{ book.getTitel }} </h4>
        <img src="{{ book.getImage }}">
        <p> Author: {{ book.getAuthor }}</p>
        <p> ISBN: {{ book.getIsbn }} </p>
    </a>

    <a href="{{ path('update_book_id', { 'id': book.getId }) }}">Update book info</a>
    <a href="{{ path('delete_book_id', { 'id': book.getId }) }}">Delete book</a>
</div>

{% endfor %}
{% endblock %}